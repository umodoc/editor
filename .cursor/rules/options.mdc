---
description: Context for working with editor options and configuration
globs: src/options/**
alwaysApply: false
---

# Options Context

When working with files in `src/options/`, read `documentation/options-and-config.md` for:
- Three-layer config merge: defaults → global options → component props
- ObjectSchema validation in schema.js
- All available options with types and defaults
- Dicts system (fonts, colors, lineHeights, pageSizes, symbols, emojis)
- How to add a new option (config + schema + component usage)
